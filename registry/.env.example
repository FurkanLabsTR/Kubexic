# Kubex Registry — Environment Variables
# Copy this file to .env and fill in the values.

# Secret key for signing API tokens (generate with: openssl rand -hex 32)
REGISTRY_SECRET=

# D1 database ID (set after running `wrangler d1 create`)
DATABASE_ID=

# R2 bucket name
BUCKET_NAME=kubex-registry-bucket
